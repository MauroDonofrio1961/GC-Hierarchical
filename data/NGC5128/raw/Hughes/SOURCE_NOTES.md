# Source notes

These two CSV files were copied from the validated
`NGC5128_three_component_v5` package supplied in this project conversation.

- `ngc5128_table1_parsed.csv`: 40,502 catalogue rows.
- `confirmed_gc_match_table.csv`: 557 confirmed-cluster rows.

The files are treated as immutable raw inputs. GC-Hierarchical writes all
derived catalogues to `data/NGC5128/processed/`.
